# 1. โฟลเดอร์ไลบรารี (กวาดรวบทั้ง Root, Client และ Server โดยอัตโนมัติ)
node_modules

# 2. โฟลเดอร์ที่ได้จากการคอมไพล์หรือ Build
dist
dist-ssr

# 3. ไฟล์ความลับ (สำคัญมาก! ป้องกัน API URL หรือรหัสผ่านหลุดขึ้น GitHub)
.env
.env.*

# 4. ไฟล์ขยะจากระบบปฏิบัติการ
.DS_Store
Thumbs.db